{
  "name": "lid-progress",
  "version": "1.0.0",
  "description": "This action creates a request to the Labeled Image Deployment api server The action will monitor for success or failure. The completion status is then returned so the workflow can make recovery decisions if required.",
  "main": "progress.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "axios": "^1.4.0"
  }
}
